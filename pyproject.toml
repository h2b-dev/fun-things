[project]
name = "tiny-dungeon"
version = "0.1.0"
description = "A tiny roguelike dungeon crawler in the terminal"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
